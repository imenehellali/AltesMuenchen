using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Events;

public class FightTaskComponent : TaskComponent
{
    public FightTaskComponent(string taskName, string taskDescription, bool taskFinished, bool taskStarted, bool taskInProgress) : base(taskName, taskDescription, taskFinished, taskStarted, taskInProgress)
    {
    }
    // Start is called before the first frame update
    void Start()
    {
    
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

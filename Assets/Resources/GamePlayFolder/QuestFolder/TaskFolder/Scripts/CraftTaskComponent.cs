using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CraftTaskComponent : TaskComponent
{
    public CraftTaskComponent(string taskName, string taskDescription, bool taskFinished, bool taskStarted, bool taskInProgress) : base(taskName, taskDescription, taskFinished, taskStarted, taskInProgress)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

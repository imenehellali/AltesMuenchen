using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class QuestDisplayStackingCompoent : MonoBehaviour
{
    
    public void StackQuests()
    {
        //here check if there are already existing quests being displayed
        //if yes take the last child Recttransform Y and substract appropritately
/* if (task._taskFinished) _tasks[_i].DisplaySingleTask(task._taskName, task._taskDescription, _finishedTaskIcon);
    else if (task._taskInProgress) _tasks[_i].DisplaySingleTask(task._taskName, task._taskDescription, _inProgressTaskIcon);
    _tasks[_i].SetRectTransform(_i > 0?-20f: _tasks[_i-1].GetPrevTaskYPos()-200f-50f);
    _i++;*/
}
}
